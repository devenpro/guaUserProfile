{
  "name": "gua-user-profile",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Drupal 11 add-on: user AI provider configuration. Producer of field_llm_config consumed by every other GoUltra AI app. Vanilla JS/CSS, concatenated build, served via jsDelivr.",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/build-component-index.mjs",
    "build:bundle": "node scripts/build.mjs",
    "build:index": "node scripts/build-component-index.mjs"
  }
}
